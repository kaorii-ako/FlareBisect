# FlareBisect
 FlareBisect finds the commit that made your tests flaky, not just the one that broke them, bisects on flake-rate drift instead of pass/fail, then flares the culprit with an AI-written root cause.
